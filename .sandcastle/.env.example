# Claude Code OAuth token — get one by running `claude setup-token` on your host.
# Lets the agent use your Claude subscription instead of an API key.
CLAUDE_CODE_OAUTH_TOKEN=
# Or use an Anthropic API key instead — uncomment and fill in:
# ANTHROPIC_API_KEY=
# See .sandcastle/SETUP_ISSUE_TRACKER.md
# This file holds only the secret. Non-secret Linear routing (team, project,
# workflow-state UUIDs, gate labels) lives in the committed .sandcastle/linear.env.
LINEAR_API_KEY=
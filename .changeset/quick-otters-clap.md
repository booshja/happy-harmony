---
"happy-harmony": minor
---

JANDES-87: Add basic authentication (BetterAuth) with login/signup routes, session handling, and authenticated route guards, plus an E2E auth test suite. Only require Sentry source-map credentials for production builds so the Playwright dev server runs in CI without them.
